# MyServiceHub Customer Portal with Multi-Factor Authentication
# Python Dependencies for Flask Application with MFA Support

# Core Flask Framework
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# Environment and Configuration
python-dotenv==1.0.0

# Multi-Factor Authentication Libraries
pyotp==2.8.0                    # Time-based One-Time Password (TOTP) for Google Authenticator
qrcode==7.4.2                   # QR Code generation for TOTP setup
Pillow==10.0.0                  # Image processing for QR codes

# SMS and Communications
twilio==8.10.0                  # SMS OTP delivery via Twilio

# Additional Security and Utilities
cryptography==41.0.7            # Enhanced cryptographic functions
itsdangerous==2.1.2             # Secure token generation
passlib==1.7.4                  # Advanced password hashing

# Database and Data Processing
sqlite3                         # Built-in with Python (SQLite database)

# Optional: Enhanced Email Support
email-validator==2.1.0          # Email validation
smtplib                         # Built-in with Python (SMTP email)

# Development and Testing
pytest==7.4.3                   # Testing framework (optional)
pytest-flask==1.3.0             # Flask testing utilities (optional)

# JSON Web Tokens (Optional for advanced authentication)
PyJWT==2.8.0                    # JWT token support

# Rate Limiting and Security
Flask-Limiter==3.5.0            # API rate limiting
bcrypt==4.1.2                   # Advanced password hashing

# HTTP Requests (for external API calls)
requests==2.31.0                # HTTP library for API calls

# Date and Time Utilities
python-dateutil==2.8.2          # Enhanced date/time handling

# File Upload Security
python-magic==0.4.27            # File type detection
